<div class="event-grid" id="{{ _id }}">
	<img class="img-rounded" data-url="/{{ slug }}" src="/thumb/{{ slug }}/{{ thumb }}?size={{ thumbSize }}" data-file-count="{{ files.length }}" />
	<small>{{ files.length }}</small>
	<h5>{{ name }}</h5>
</div>
